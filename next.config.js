/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "7luxja62pntrd0xo.public.blob.vercel-storage.com",
      },
    ],
  },
};

module.exports = nextConfig;
